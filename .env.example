DB_USER=bdg
DB_PASSWORD=bdg
DB_NAME=budget_tracker
DB_PORT=5433

DATABASE_URL=postgresql://bdg:bdg@localhost:5433/budget_tracker?schema=public

# ── Integrations (Phase J) ────────────────────────────────────────────────────
# Primary key for integration credentials (AES-256-GCM).
# Must be a 64-char hex string (32 bytes).
# Generate with: openssl rand -hex 32
INTEGRATIONS_ENC_KEY=

# Optional: additional key versions for rotation.
# To rotate: add the new key as V2 (or V3, etc.), run:
#   tsx scripts/rotate-integrations-key.ts
# After all rows are migrated, the old key env var can be removed.
# INTEGRATIONS_ENC_KEY_V2=
# INTEGRATIONS_ENC_KEY_V3=

# Set to "true" to enable the bank integrations UI at /settings/integrations.
# Admin-only gate — keeps the feature hidden in production until ready.
ADMIN_INTEGRATIONS=

# ── Playwright (Tinkoff retail adapter) ───────────────────────────────────────
# Where persistent browser profiles are stored, one subdir per credentialId.
# Defaults to /var/lib/budget-tracker/playwright-profiles (matches prod volume).
# For local dev set to a writable path, e.g. /tmp/bdg-pw-profiles.
# PLAYWRIGHT_PROFILES_DIR=/tmp/bdg-pw-profiles

# Force visible Chromium for selector debugging in dev. "false" or "0" → visible.
# Anything else (including unset) → headless. Production should leave this unset.
# PLAYWRIGHT_HEADLESS=false

# Set to "false" to suppress heavy Playwright diagnostics (body/html dumps, screen-attribute scans). Default: on.
# DEBUG_PLAYWRIGHT=false

# ── Bybit Card integration ────────────────────────────────────────────────────
# Read-only HMAC API key for pulling Bybit Card transactions. Create at
# https://www.bybit.com/app/user/api-management — bind to prod IP (bessrochno),
# permissions: BitCard → Orders (read-only). Do NOT enable trade/withdraw scopes.
BYBIT_API_KEY=
BYBIT_API_SECRET=
# Optional: alternate origin for Bybit API requests (proxy to bypass region block).
BYBIT_PROXY_URL=
