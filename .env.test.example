# Test database configuration
# Points to a SEPARATE database (budget_tracker_test) on the same local Postgres instance.
# NEVER run tests against the dev database or production.
#
# This file is COMMITTED to the repo as documentation.
# Developers copy it to .env.test and customize if needed (normally not required).

DATABASE_URL=postgresql://bdg:bdg@localhost:5433/budget_tracker_test?schema=public
